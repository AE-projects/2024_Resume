<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parallax Background</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .parallax-container {
            position: relative;
            overflow: hidden;
            width: 100%;
            height: 100vh;
        }

        .parallax-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 150%;
            background-image: url('https://source.unsplash.com/1600x900/?nature');
            background-size: cover;
            background-position: center;
            transform: translate3d(0, 0, 0);
            z-index: -1;
        }

        .content {
            position: relative;
            z-index: 1;
            text-align: center;
            padding-top: 50px;
        }

        h1 {
            color: #fff;
            font-size: 3em;
        }

        p {
            color: #fff;
            font-size: 1.2em;
        }
    </style>
</head>
<body>

    <div class="parallax-container">
        <div class="parallax-bg"></div>
        <div class="content">
            <h1>Welcome to Parallax Background</h1>
            <p>This is some content on top of the parallax background.</p>
            <p>Scroll down to see the effect!</p>
        </div>
    </div>

    <div class="content">
        <h2>Main Content Goes Here</h2>
        <p>This is the main content area.</p>
        <p>It can have multiple sections with different content.</p>
    </div>

    <script>
        // Parallax effect
        window.addEventListener('scroll', function () {
            var scrollPosition = window.pageYOffset;
            var parallaxElements = document.querySelectorAll('.parallax-bg');

            parallaxElements.forEach(function (element) {
                var speed = parseFloat(element.getAttribute('data-speed'));
                var yPos = -scrollPosition * speed;
                element.style.transform = 'translate3d(0, ' + yPos + 'px, 0)';
            });
        });
    </script>

</body>
</html>
